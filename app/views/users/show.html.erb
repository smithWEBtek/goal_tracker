<h1>Hello, <%= current_user.username %></h1>

<h3>Your Current Goals</h3>
  <ul>
    <%= render(@incomplete_goals) || content_tag(:li, "No goals in progress") %>
  </ul>

<h3>Your Achievements</h3>
  <ul>
    <%= render(@completed_goals) || content_tag(:li, "No completed goals available") %>
  </ul>

<p><%= link_to "Create a Goal", new_goal_path %></p>
