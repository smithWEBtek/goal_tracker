<% if object.errors.any? %>
  <h2><small>The form contains <%= pluralize(object.errors.count, "error") %>:</small></h2>
    <ul>
      <% object.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
<% end %>
